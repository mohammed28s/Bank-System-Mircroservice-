spring.application.name=User-Service

# Sqlite Database configuration (match the keys used in your Java code)
driverClassName=org.sqlite.JDBC
#url=jdbc:sqlite:memory:myDb?cache=shared
url=jdbc:sqlite:jdbc:sqlite:/Users/mohd24s_tkd/Desktop/SpringBoot-Microservices /Services Database /auth_service.db
#username=${USERNAME}
#password=${PASSWORD}

# Hibernate/JPA Configuration
spring.jpa.database-platform=org.hibernate.community.dialect.SQLiteDialect
spring.jpa.hibernate.ddl-auto=validate
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type=TRACE

# JWT Configuration
jwt.secret=${JWT_SECRET}
jwt.expiration-ms=${JWT_EXPIRATION}  # 15 mins

server.port=8085